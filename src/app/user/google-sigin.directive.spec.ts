import { GoogleSiginDirective } from './google-sigin.directive';

describe('GoogleSiginDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSiginDirective();
    expect(directive).toBeTruthy();
  });
});
